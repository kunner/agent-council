services:
  server:
    build:
      context: .
      dockerfile: packages/server/Dockerfile
    ports:
      - "3001:3001"
    env_file:
      - packages/server/.env
    volumes:
      - workspace:/srv/workspace
    restart: unless-stopped

volumes:
  workspace:
